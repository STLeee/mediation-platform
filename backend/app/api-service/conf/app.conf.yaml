server:
  host: localhost
  port: 8080
  gin_mode: debug

service:
  name: api-service
  env: test

auth_service:
  firebase:
    project_id: mediation-platform-test
    key_file: ../../../key/gcp-sa-key.test.json
    emulator_host: localhost:9099
